# chi-square-Test-on-Lung-Capacity
Chi Square Test is applied when we have 2 categorical variables from the same population. It is used to determine  whether there is significant association between them
